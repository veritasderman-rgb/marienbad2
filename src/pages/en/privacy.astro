---
import PillarPage from '../../components/PillarPage.astro'
import { getEntry } from 'astro:content'
const entry = await getEntry('pages', 'en-privacy')
const Content = entry ? (await entry.render()).Content : null
---
<PillarPage locale="en" title="Privacy Policy" eyebrow="Legal" coverTone="warm"
  breadcrumbs={[{label:'Home',href:'/en/'},{label:'Home',href:'/en/'},{label:'Privacy Policy'}]}>
  {Content ? <Content /> : <p>Content to follow. This page will be populated with Ensana content.</p>}
</PillarPage>
